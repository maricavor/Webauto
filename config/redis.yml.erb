defaults: &defaults
  host: 127.0.0.1
  port: 6379

development:
  <<: *defaults 

staging:
  <<: *defaults

production:
  host: <%= ENV["OPENSHIFT_REDIS_HOST"] %>
  port: <%= ENV["OPENSHIFT_REDIS_PORT"] %>
  password: <%= ENV["REDIS_PASSWORD"] %>
  thread_safe: true

