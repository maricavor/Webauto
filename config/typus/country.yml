# Use the README file as a reference to customize settings.

Country:
  fields:
    default: name
    form: name
  relationships: translations, states, cities, vehicles, users
  application: Application
