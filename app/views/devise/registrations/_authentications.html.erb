  <% unless authentications.empty? %>
 <div class="row-fluid">
 <div class="span12">
<p><strong><%= t("registrations.can_sign_in")%></strong></p>
    <% for authentication in authentications %>
     <div class="authentication" id="<%= authentication.provider.to_s.downcase %>_auth">
     <%= image_tag("/#{authentication.provider.to_s.downcase}.png", :border => 0,:size=>"32x32") %>
      <div class="provider">  <%= authentication.provider_name %></div>
      <div class="uid"><%= authentication.uid %></div>
      <% if authentications.count>1 %>
       <%= link_to "&#215;".html_safe, authentication, :confirm => t("registrations.sure"), :method => :delete, :class => "remove",:remote=>true %>
       <% else %>
       <% if !current_user.encrypted_password.blank? %>
       <%= link_to "X", authentication, :confirm => t("registrations.sure"), :method => :delete, :class => "remove",:remote=>true %>
       <% end %>
       <% end %>
     </div>
     <% end %>

      </div>
       </div>
  <% if authentications.count < User.omniauth_providers.count %>
   <div class="row-fluid">
 <div class="span12">
   <p><strong><%= t("registrations.add_more")%></p>
      </div>
       </div>
   <% end %>
<% else %>
   <div class="row-fluid">
 <div class="span12">
   <p><strong><%= t("registrations.add_service")%></p>
      </div>
       </div>


       <% end %>
        