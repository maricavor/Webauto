<%= form_for(@compared_item) do |f| %>
  <% if @compared_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compared_item.errors.count, "error") %> prohibited this compared_item from being saved:</h2>

      <ul>
      <% @compared_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
