
<table class="table table-bordered table-condensed">
	<thead>
	    <tr>
	      <th><%= t("orders.your_cart")%></th>
	      <th style="text-align: right;"><%= t("orders.total")%></th>
	    </tr>
	  </thead>
	   <tbody>
<% @order.line_items.each do |item| %>
<tr class="info">
<td><%= item.service.title %></td>
<td style="text-align: right;"><%= t("orders.free") %></td>
</tr>
<% end %>
<tr>
<td style="text-align: right;"><strong><%= t("orders.order_total")%></strong></td>
<td style="text-align: right;"><%= t("orders.free") %></td>
</tr>
  </tbody>
</table>