<% if @photo.new_record? %>
alert("Failed to upload image: <%= j @photo.errors.full_messages.join(', ').html_safe %>"); 
<% else %>

$("#photos").append("<%= j(render(@photo)) %>");

<% end %>
$('.site-message').html("<%= j(render partial:'layouts/messages')%>");
