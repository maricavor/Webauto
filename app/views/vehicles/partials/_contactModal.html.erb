

<!-- Modal -->
<div id="contactModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="contactModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <% if user.is_dealer %>

    <h3 id="contactModalLabel"><%= t("dealers.index.email_dealer") %></h3>
    <% else %>

     <h3 id="contactModalLabel"><%= t("dealers.index.email_dealer") %></h3>
     <% end %>
  </div>
  <div class="modal-body">
	  <%= render partial: "inquiries/form",locals: {mode: "contact_seller"}  %>
     </div>
  <div class="modal-footer">
    <div class="preloader"></div>
    <button class="contact_seller_btn btn btn-info"><%= t("inquiries.send") %></button>
    <button class="btn" data-dismiss="modal" aria-hidden="true"><%= t("inquiries.close") %></button>
  </div>
</div>