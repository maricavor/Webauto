<% content_for :title,@title %>
<div class="row-fluid">
  <div class="span12">
    <div class="page-header">
      <h2>Browse all dealers on Webauto</h2>
    </div>
        <p></p>
          <table class="table table-hover">
      <thead>
        <tr>
        	<th></th>
          <th>NAME</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @users.each do |u| %>
        <% picture=u.dealer_picture  %>
          <tr id="user_<%= u.id %>">
            <td width="150">
       <div class="thumbnail">              
      <% if picture %>
      <%= image_tag(picture.file_url(:original)) %>
    <% else %>
      <%= image_tag "/thumbnail.png",size: "300x200" %>
    <% end %>
    </div>
</td>
<td>
            <strong>
                   <%= u.company_name %>
            </strong>  
            </td>  
            <td>
            <%= link_to "View stock", "#" %>
            </td>        
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>